name: Deploy to EC2

on:
  push:
    branches:
      - main  # Asegúrate de cambiar esto si usas otra rama principal

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: 🚀 Checkout del código
      uses: actions/checkout@v3

    - name: 🔑 Acceder a EC2 y desplegar
      uses: appleboy/ssh-action@v0.1.5
      with:
        host: ${{ secrets.EC2_HOST }}
        username: ${{ secrets.EC2_USER }}
        key: ${{ secrets.EC2_PRIVATE_KEY }}
        script: |
          echo "🚀 Iniciando despliegue en EC2..."

          # Crear el directorio de la app si no existe
          mkdir -p /home/${{ secrets.EC2_USER }}/ms-login
          cd /home/${{ secrets.EC2_USER }}/ms-login

          echo "📦 Clonando o actualizando código fuente..."
          if [ -d "ms-login" ]; then
            cd ms-login && git reset --hard && git pull origin master
          else
            git clone -b master https://github.com/argalarza/ms-login.git ms-login
            cd ms-login
          fi

          echo "📑 Creando archivo .env en el servidor..."
          cat <<EOF > .env
          DB_HOST=${{ secrets.DB_HOST }}
          DB_PORT=${{ secrets.DB_PORT }}
          DB_USER=${{ secrets.DB_USER }}
          DB_PASSWORD=${{ secrets.DB_PASSWORD }}
          DB_NAME=${{ secrets.DB_NAME }}
          JWT_SECRET=${{ secrets.JWT_SECRET }}
          PORT=3000
          EOF

          echo "🛠️ Construyendo y levantando los contenedores..."
          docker-compose -f docker-compose.prod.yml down --remove-orphans
          docker-compose -f docker-compose.prod.yml up --build -d

          echo "🔍 Verificando que el servicio está corriendo..."
          docker ps | grep ms-login || { echo "⚠️ ERROR: El contenedor no se inició correctamente."; exit 1; }

          echo "✅ Despliegue completado exitosamente."
